import LandingPage from "@/app/_components/landing/LandingPage";

/** /feedback — the landing scrolled to the feedback/reviews section (#feedback). */
export default function FeedbackPage() {
  return <LandingPage scrollTo="feedback" />;
}
