import LandingPage from "@/app/_components/landing/LandingPage";

/** /order — the landing with the order drawer opened straight to the cart. */
export default function OrderEntry() {
  return <LandingPage openOrder />;
}
