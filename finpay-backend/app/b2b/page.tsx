import LandingPage from "@/app/_components/landing/LandingPage";

/** /b2b — the landing scrolled to the B2B / wholesale section (#cafes). */
export default function B2BPage() {
  return <LandingPage scrollTo="cafes" />;
}
