import LandingPage from "@/app/_components/landing/LandingPage";

/** /menu — the landing scrolled to the menu section (#menu). */
export default function MenuPage() {
  return <LandingPage scrollTo="menu" />;
}
